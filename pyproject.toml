[project]
name = "personal-database-mcp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ngocthinh09", email = "volengocthinh09@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
personal-database-mcp-server = "personal_database_mcp_server:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
